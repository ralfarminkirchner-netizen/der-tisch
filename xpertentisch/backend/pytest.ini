[pytest]
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
addopts = -m "not live"
markers =
    live: Tests gegen die echten Provider-APIs (nur mit Zugangsdaten, kostenpflichtig)
filterwarnings =
    ignore::DeprecationWarning
